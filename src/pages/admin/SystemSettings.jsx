const SystemSettings = () => (
  <div>
    <h1 className="text-2xl font-bold mb-4">⚙️ System Settings (Coming Soon)</h1>
    <p>Future options like user roles, account controls, etc.</p>
  </div>
);

export default SystemSettings;
